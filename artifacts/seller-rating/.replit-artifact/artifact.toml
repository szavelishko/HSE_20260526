kind = "web"
previewPath = "/"
title = "Оценка продавцов маркетплейса"
version = "1.0.0"
id = "artifacts/seller-rating"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24418

[services.development]
run = "pnpm --filter @workspace/seller-rating run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/seller-rating", "run", "build" ]
publicDir = "artifacts/seller-rating/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24418"
BASE_PATH = "/"
